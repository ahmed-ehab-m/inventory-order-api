spring.application.name=inventory-order-api

# ===== Database =====
spring.datasource.url=jdbc:mysql://mysqldb:3306/inventory_order_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root

# === Dev Tools ===
spring.devtools.restart.enabled=false

# === Development ===
spring.profiles.active=dev

# === Swagger ===
springdoc.swagger-ui.path=/docs
springdoc.api-docs.path=/api-docs-json
# UI
# sort by method type
springdoc.swagger-ui.operations-sorter=method
# sort Controllers by alpha
springdoc.swagger-ui.tags-sorter=alpha

# === disbale external monitoring ===
spring.jmx.enabled=false

# === SQL Console ===
# false because it use System.out and ignore log4j2 settings we created
#  so i can't save it into file
spring.jpa.show-sql=false

spring.jpa.properties.hibernate.format_sql=true
# highlight reserved words
spring.jpa.properties.hibernate.highlight_sql=true

# === i18n ===
spring.messages.basename=i18n/messages
spring.messages.encoding=UTF-8

#=== actuator ===
# show only important endpoints
management.endpoints.web.exposure.include=health,info,metrics
# show details and status of DB and any integrated platform or service
management.endpoint.health.show-details=always
# close old way to present actuators
management.endpoints.jmx.exposure.exclude=*

# === flyway ===
# make flyway resposible for creating tables in db not hibernate
spring.jpa.hibernate.ddl-auto=validate

# === Cloudinary ===
cloudinary.cloud-name=${CLOUDINARY_NAME}
cloudinary.api-key=${CLOUDINARY_API_KEY}
cloudinary.api-secret=${CLOUDINARY_API_SECRET}

# === Files ===
spring.servlet.multipart.max-file-size=5MB
# if upload more than file
spring.servlet.multipart.max-request-size=5MB

# === Seed Data ===
app.seeder.enabled=true

# === JWT Service ===
jwt.secret=${JWT_SECRET}
# 10 Hours
jwt.expiration=36000000

# === OAuth2 ===
# GOOGLE
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
# GITHUB
spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}

# === Admin Account ===
app.admin.email=${ADMIN_EMAIL}
app.admin.password=${ADMIN_PASSWORD}

# === Front-end URL ===
app.frontend-url=http://localhost:8080/api/v1/auth/success

